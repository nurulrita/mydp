{% load staticfiles%}
<!DOCTYPE html>
<html>
    
    <head>
	<title>Library</title>
    </head>

    <body>
	<h1>Welcome to Nuthanu Library</h1>
    
        {% if genres %}
            <ul>
                {% for genre in genres %}
                    <li><a href="{% url 'library:genre'  genre.slug %}">{{ genre.name }}</a></li>
                {% endfor %}
            </ul>
	{% else %}
	    <strong>There are no genres present.</strong>
        {% endif %}

    </body>

</html>
